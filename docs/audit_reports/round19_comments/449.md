## 📈 Round 19 progress note (stays open — regression-guarded program)

Dead-code removal (escrow, delivery/rider fleet, diagram-parser, 5 integration adapters) intentionally NOT executed this round: the new Critical Invariant Guard (#476, shipped today) should first accumulate one full pipeline cycle proving the marker registry is stable, and the removal deserves its own regression-sweep session (full backend matrix + 4-node live check). Today's session closed 7 upstream operational issues instead. Suggested next-session sequence: (1) `git grep` liveness audit per module listed here, (2) removal commit(s) under the commit-sizing cap (#475), (3) full CI + live sweep, (4) close.
