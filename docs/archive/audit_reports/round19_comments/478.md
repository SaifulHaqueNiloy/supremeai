## ✅ Resolved — fail-fast migration gate + readiness schema gate (verified live)

### Shipped (commits e4ef6fe → 62f7862, live on Render primary)

**1. Migration gate runs BEFORE deploy** (`ci-deploy-production.yml`, job `migration-gate`, `deploy-core` now `needs` it)
- **Offline chain verification (always)**: `alembic upgrade head --sql` — proves the full migration chain loads and compiles. Local verification: exit 0, **2015 SQL statements generated, single head `task_records_0001`**. This required making **5 revision files offline-safe** (runtime `inspect()` only on live mode; identical live semantics): `2f7b3c5f620e`, `7c4d9e1f2a3b`, `j9k0l1m2n3o4`, `k1l2m3n4o5p6`, `k5l6m7n8o9p0`.
- **Live `alembic upgrade head`** executes automatically the moment secret `SUPABASE_DATABASE_URL_WRITER` exists — and a broken migration then blocks deploys *before traffic shifts*.
- **Progressive enforcement** (AGENTS.md risk doctrine / #431 Option-B precedent): writer secret absent today → the job PASSES with an amber step-summary showing the exact owner command; repo variable `MIGRATION_GATE_ENFORCE=true` flips that to a hard fail once the owner opts in. (A hard fail was deliberately NOT shipped unconfigured — it would have blocked all deploys including the repo bot's.)

**2. Readiness reports schema state — verified live**
```
GET /api/v1/health/ready (Render primary @ 62f7862, 2026-09-19T07:42Z)
→ 200 {"status":"ready", "schema":{"checked":true,"missing":[],
        "present":["automation_executions","ai_memory","task_checkpoints"]}}
```
- `backend/core/db_schema_gate.py`: read-only PostgREST probe of the required tables, 60 s cache, resolves creds via the **settings SSoT** (zero-hardcode scanner compliant — scanner SUCCESS verified locally), never emits credentials, never crashes the probe. Wired into BOTH readiness implementations (`api/routes/health.py` + the canonical `core/health_routes.py`).
- Production, core role, missing required table(s) → **503 `not_ready`** with the exact missing-table names (verified by unit-level failure-path injection; honest-unknown stays non-failing rather than fabricating either color).

**3. No more silent skip** — `core/lifespan.py` logs **CRITICAL** (not a warning) when bootstrap fails/times out in production; no crash-loop (Sep-14 precedent respected — the readiness gate owns fail-closed).

**4. Required tables verified live right now**: `automation_executions` ✓ `ai_memory` ✓ — and note: the literal `checkpoints` table **does not exist**; the canonical checkpoint table is **`task_checkpoints`** (also what `schema_contract.yaml` names), which the gate uses.

**5. MANUAL_STEPS 7.10 → RESOLVED; 7.9 → PARTIALLY RESOLVED** — the remaining owner step is exactly one: set `SUPABASE_DATABASE_URL_WRITER` in Render (direct connection string, port 5432). Every downstream behavior (live migration execution + optional hard enforcement) then activates without further code changes.

### Regression evidence
- CI Pipeline **SUCCESS** on df7dc07 (includes gate + all backend changes): Backend Tests (fast/core-unit/core-support/services), Integration, Coverage, Contract, Advanced Pre-Merge Checks (incl. the zero-hardcode scanner) — all green.
- Live sweep after deploy: **4/4 nodes `/health` → 200**; deep health healthy (database+memory).
- An earlier pipeline failure (ruff format on 3 files + hardcode scanner on `os.getenv`) was caught and fixed during this round — the gates did their job.

### 🇧🇩 বাংলা সারসংক্ষেপ
মাইগ্রেশন গেট এখন deploy-এর আগে চলে: অফলাইন চেইন-ভেরিফিকেশন সবসময় (২০১৫ SQL স্টেটমেন্ট, সিঙ্গেল হেড), রাইটার সিক্রেট সেট হলেই লাইভ `alembic upgrade head`। রেডিনেস প্রোবে স্কিমা-স্টেট ব্লক যোগ হয়েছে — লাইভ যাচাইকৃত: ৩/৩ টেবিল প্রেজেন্ট, মিসিং হলে core role-এ ৫০৩। প্রোডাকশনে বুটস্ট্র্যাপ স্কিপ এখন CRITICAL লগ হয় (নীরব সাফল্য নয়)। ওনারের জন্য অবশিষ্ট একটিই ধাপ: Render-এ `SUPABASE_DATABASE_URL_WRITER` সেট করা। ইস্যুটি বন্ধ হচ্ছে।
