## 🔴 নতুন প্রমাণ (Round 14, 2026-09-18): universal-auth login নিজেই 500 করছে

**বাংলা:** আজ universal-auth login এন্ডপয়েন্ট (`POST /api/v1/auth/universal-auth/login`) এক ঘণ্টার ব্যবধানে ৩টি আলাদা অনুরোধে `500 InternalServerError` ফেরত দিয়েছে (reqId: req-us-OEDrDs1IKgauAF, req-us-Eb4yuiNCC4Q1kC, req-us-FwHtlSyyfG9IUB — প্রতিটি ৫ সেকেন্ড ব্যবধানে রিট্রাই)। একই ক্রেডেনশিয়াল আজ দুপুরে কাজ করছিল। এটি blind-index সমস্যার চেয়েও গভীর vendor-side instability — vendor ticket-এ এই reqId-গুলোও যোগ করুন।

**English:** Login 500s are upstream of the blind-index defect (no token is even issued). REST v1 routes 404 (per original report) AND now login itself is flapping. Meanwhile CI's Infisical secrets-action path still imports successfully (different identity), so production boot is unaffected. Recommendation unchanged: vendor ticket with the reqIds + the original blind-index evidence; add a synthetic canary check (login → list → write probe → delete) run from CI every 6h.
