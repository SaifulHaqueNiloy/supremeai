# Experimental plugins — capability graph honesty layer.
# These plugins are registered but not yet fully implemented.
# They return graceful "unavailable" responses instead of crashing with NotImplementedError.
# Lifecycle: IDEA → measured → connected (see MASTER_PLAN.md Phase 1 health-probe promotion).
