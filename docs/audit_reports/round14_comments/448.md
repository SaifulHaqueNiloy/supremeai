## ✅ ফিক্স হয়েছে — commit `dd40ed9e` (production-এ live: c204db97)

**বাংলা:** আগে `/api/v1/sandbox/*` প্রতিটি কলে `ValueError("Local sandbox provider is not permitted in production")` → HTTP 500; mock সেশনও তৈরি হত। এখন কনস্ট্রাকশন ব্যর্থ হলে প্রতিটি এন্ডপয়েন্ট সৎ **503 `SANDBOX_UNAVAILABLE`** দেয় (প্রসেসে একবারই CRITICAL লগ), actionable বার্তা সহ (E2B/RUNPOD কী কনফিগার করতে হবে)। Production-এ mock-mode create আর পৌঁছায়ই না।

**English:** `sandbox_api.py`: `_get_manager()` catches the ValueError → None; `_require_manager()` raises 503 on all 5 endpoints; `services/sandbox_service.py` `execute()` returns `NOT_EXECUTED` (never fabricated SUCCESS). Frontend already renders the `mock` flag honestly. **Owner action:** আসল sandbox চাইলে E2B বা RUNPOD sandbox কী যোগ করুন।
