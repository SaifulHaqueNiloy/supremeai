## ✅ ফিক্স হয়েছে — commit `dd40ed9e` (production-এ live: c204db97)

**বাংলা:** আগে প্রতিটি ছবির জন্য একই বানানো উত্তর ("Identified 3-tier microservice…", confidence 0.94) ফেরত যেত — একটা বাইটও পড়া হত না। এখন `vision_service.py` সত্যিকারের Gemini vision কল করে (inline base64 image, `settings.model_vision` মডেল); কী না থাকলে সৎ `{"status": "unavailable", "reason": "VISION_NOT_CONFIGURED"}` — বানানো confidence বা detected_objects আর কখনো নয়।

**English:** Real Gemini `:generateContent` call with `inline_data` (magic-byte MIME sniffing); error path surfaces provider HTTP status; no fabricated fields ever. py_compile + ruff clean. **Owner action:** GEMINI_API_KEY vault/Render env-এ থাকলে vision এখন স্বয়ংক্রিয়ভাবে সত্যিকারের বিশ্লেষণ দেবে।
