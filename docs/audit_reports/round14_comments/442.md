## 🔧 মূল ফিক্স ল্যান্ড করেছে — commit `c204db97` (production-এ live)

**বাংলা:** Module 01-এর মূল অংশ ইমপ্লিমেন্ট হয়েছে: (১) `_HAS_SENTENCE_TRANSFORMERS` আর hardcoded False নয় (find_spec প্রোব; LOW_MEMORY_MODE-তে মডেল লোড বন্ধ থাকবে, তবে এখন জোরে ঘোষণা করা হয়); (২) **আসল semantic embeddings** — Cloudflare Workers AI `@cf/baai/bge-small-en-v1.5` (দিনে ~১০k ফ্রি) যখন CLOUDFLARE_API_TOKEN + CLOUDFLARE_ACCOUNT_ID কনফিগার করা থাকে; (৩) hash fallback আপনার ডিজাইন অনুযায়ী ঠিক করা হয়েছে — বাংলা+ইংরেজি stopword ফিল্টার, sublinear TF (1+log), 3-probe signed hashing; (৪) DEGRADED মোড এখন জোরে ঘোষিত।

**English verification (founder's own test):** old algo REPRODUCED the inversion (relevant 0.2309 < irrelevant 0.2697); new algo: **relevant 0.3592 vs irrelevant 0.0000 — inversion eliminated**. Determinism (blake2b) preserved; 384-dim contract holds; `vector_search` ranks the relevant doc first. `memory_service.get_embedding` routes through `embed_for_pgvector`, so the whole memory layer upgrades.

**Remaining (issue stays open):** (1) owner wires CF creds into Render env (GitHub secrets already have CLOUDFLARE_API_TOKEN/ACCOUNT_ID); (2) reindex stored hash vectors after provider switch (mixed-space prevention is now structural — one sync provider shared by query+document paths); (3) Supabase `ai_memory` centralization per Module 01 plan.
