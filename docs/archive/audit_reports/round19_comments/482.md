## ✅ Resolved — PRODUCTION_URL configured + live smoke verified green

### What was done (2026-09-19)
1. **Repository variable set** — `PRODUCTION_URL = https://supremeai-primary-node.onrender.com` (set 2026-09-18T23:26:56Z via Actions:write-capable token).
2. **Workflow resolve-chain corrected** (commit `c4e4512`): the owner override previously mapped *both* UI and API origins to the override URL — but the override is an **API-only origin** (`GET /` returns JSON, not SPA HTML), so the Layer A SPA probe would false-fail. The override now acts as the **API entry**, while the UI origin derives from `FIREBASE_PROJECT_ID` — exactly the real browser chain (SPA → direct CORS call). Fail-closed preflight assertion retained.
3. **Live smoke dispatched on `main@c4e4512`** — [run #7](https://github.com/SaifulHaqueNiloy/supremeai/actions/runs/35426837424): **✅ PASS — live production path verified (customer chain + API)**
   - Layer A origin: `FIREBASE_PROJECT_ID-derived (*.web.app) — SPA hosting` → `frontend /` = 200 (170 ms, SPA served ✓); API direct w/ Origin = 200 (80 ms, CORS allow-origin ✓)
   - Layer B origin: `PRODUCTION_URL (owner override)` → `/api/v1/health/live` + `/health/ready` + `/api/billing/plans` contract ✓
4. **Evidence matrix updated** — `docs/audits/ACTIVE_AUDIT_QUEUE.md` GAP-006 → `[RESOLVED]` (no credentials exposed; probe table lives in the workflow step summary).

### Acceptance criteria check
| criterion | status |
|---|---|
| Live-smoke reaches the intended deployment | ✅ run #7 green, Layer B via `vars.PRODUCTION_URL` |
| Missing `PRODUCTION_URL` stays fail-closed w/ actionable message | ✅ preflight step retained (exit 1 + owner-action hint) |
| Evidence matrix records date + result | ✅ GAP-006 row `[RESOLVED] (2026-09-19)` + step summary |

### 🇧🇩 বাংলা সারসংক্ষেপ
`PRODUCTION_URL` রিপো ভেরিয়েবল সেট করা হয়েছে এবং workflow-এর resolve-চেইন ঠিক করা হয়েছে (override = API entry, SPA origin Firebase থেকে ডেরাইভ — ব্রাউজারের আসল পথ)। লাইভ স্মোক রান #7 `main@c4e4512`-এ **✅ PASS** (SPA ✓, CORS ✓, health live/ready ✓, billing plans ✓)। ভেরিয়েবল অনুপস্থিত থাকলে fail-closed আচরণ অক্ষুণ্ণ। ইস্যুটি বন্ধ করা হচ্ছে।
