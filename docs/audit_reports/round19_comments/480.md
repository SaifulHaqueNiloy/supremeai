## 📈 Round 19 — GAP-001 deliverable shipped: generated route→client inventory + CI ratchet (3/5 fixes landed)

### What shipped (commits d7c83cb → 9f9a489)

**Fix #1 — generated inventory (checked in)**: `scripts/ci/generate_route_client_inventory.py` (dependency-free static scanner) + committed artifacts `docs/audit_reports/route_client_inventory.{json,md}`. Live first run:

| metric | value |
|---|---|
| backend routes scanned (file/method/declared/resolved) | **536** |
| unique frontend `/api/...` references (with referencing files) | **169** |
| matched (frontend-reachable) | **268** |
| orphan backend routes | **268** across **115 families** |

Matching is alias-aware (`/api/v1/...` ↔ `/api/...`), param-normalized (`${id}` ↔ `:param` ↔ `{id}`), and credits frontend *base-path* references (e.g. a client pinned to `/api/admin` covers `admin/*` children). Assumptions are documented inside the JSON header — this replaces the manual "57 families" guess with a reproducible, per-route baseline (the difference vs the audit's 57 is scope: the scanner covers ALL route files incl. internal/ops/MCP surfaces the SPA intentionally never touches).

**Fix #2 — heuristic classification** per orphan route: `unclassified-orphan` (222 — triage: user-facing wiring vs intentional API-only), `internal` (40 — verify not publicly reachable), `admin-only` (6 — wire into admin UI or document), plus `deprecated-pattern` detection (0 today). The classification table in the markdown carries the concrete next action per bucket — owner ratification converts these into the issue's required "explicit disposition and owner".

**Fix #5 — CI contract test**: `tooling-quality` job now REGENERATES the inventory and fails when the committed artifact is stale (route surface changed without regenerating) — newly orphaned families can no longer land silently.

### Remaining for this issue's full closure (program-scale, tracked)
- **Fix #3**: wire the triaged user-facing subset through the canonical frontend service layer — the actionable queue is the `unclassified-orphan` bucket, triaged family-by-family from the JSON.
- **Fix #4**: formal deprecation (headers + docs) for routes the owner marks not-exposed.
- Owner ratification of dispositions.

### 🇧🇩 বাংলা সারসংক্ষেপ
৫৩৬ রুট স্ক্যান করে জেনারেটেড ইনভেন্টরি (JSON+MD) কমিট করা হয়েছে: ২৬৮ frontend-reachable, ২৬৮ orphan (১১৫ ফ্যামিলি; ২২২ ট্রায়াজ, ৪০ ইন্টার্নাল, ৬ অ্যাডমিন)। CI র্যাচেট যোগ হয়েছে — রুট-সারফেস বদলালে ইনভেন্টরি রিজেন করতেই হবে। বাকি: ট্রায়াজ-করা রুটগুলোর ফ্রন্টএন্ড ওয়্যারিং ও deprecation — ফ্যামিলি-ধরে ধাপে ধাপে।
