## ✅ ফিক্স সম্পন্ন — ফ্রন্টএন্ড এখন শুধু ডেলিভারযোগ্য ফিচারই দেখায় / ভুয়া affordance সরানো

**বাংলা:** চারটি ভাঙা affordance-এর প্রতিটির জন্য বাস্তব সমাধান: run-code ও vision আগেই ঠিক (Round 14); research প্যানেল এখন সত্যিকারের SSE চুক্তি বোঝে (আগে কোনো ধাপ বা রিপোর্ট রেন্ডারই হতো না); voice toggle এখন সত্যিকারের অডিও বাজায়।

**English evidence (commits 3062fd37, d290a00a + #446/#450 fixes):**
1. Run code — ALREADY-FIXED (WebContainer + honest NOT-RUN banner).
2. Vision — ALREADY-FIXED (real Gemini call or honest VISION_NOT_CONFIGURED, Round 14).
3. TTS voices — the toggle emitted `TTS_GENERATED` with **zero subscribers**; now ChatInterface fetches `/api/voice/stream_audio` with auth header and plays the blob; honest system message when provider unconfigured.
4. Research — SSE contract mismatch fixed (backend `step`/`report` now rendered; report shape mapped); `getApiBaseUrl()` replaces `window.location.origin`; #447 fake fallback removed.
5. Forge — save/execute honest (Round 14); live DebateOverlay revived via the new `/swarm/stream` route (#446); Swarm Map fed by real `/swarm-graph` (#446).
6. Capability matrix for remaining gating: `/api/v1/capabilities/runtime` (#450).

**Acceptance:** no dead affordances in user-facing UI ✓
