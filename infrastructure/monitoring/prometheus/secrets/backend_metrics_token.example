Paste a backend service/admin JWT here (single line, no newline).
This file (without the .example suffix) is mounted read-only into the
prometheus container at /etc/prometheus/secrets/backend_metrics_token and
is read per scrape — rotating the token needs no prometheus restart.
