## 🔧 আংশিক ফিক্স — pgvector ডিগ্রেডেড মোডেও জীবন্ত; বাকি ফ্ল্যাগ-সিদ্ধান্ত মালিকের

**বাংলা:** ExperienceDatabase-এর production gate আগে Supabase pgvector backend তৈরি হওয়ার **আগেই** return করত — অথচ pgvector-এর SQLite বা লোকাল ডিস্ক কিছুই লাগে না। এখন ডিগ্রেডেড মোডেও pgvector বাঁচিয়ে রাখা হয়: লেখা সত্যিই persist হয় ("writes DROPPED" আর নেই), semantic cache সত্যিকারের হিট পায়। **বাকি অংশ:** ১২টি ENABLE_* ফ্ল্যাগ এখনো false — কোনগুলো চালু হবে তা owner-এর সিদ্ধান্ত (cost/512MB RAM trade-off)।

**English evidence (commit 00c771f3):**
- `adaptive_engine/experience_db.py`: constructor's `_degraded_no_store` branch now attempts `SupabaseVectorBackend()`; `record_experience`/`find_similar` route through it when alive (writes return real id, queries return real hits) — true pass-through only when pgvector is ALSO unavailable.
- Provider chain under `LOW_MEMORY_MODE=true` rides `core.embeddings` → Cloudflare Workers AI (activated in Round 15) → LiteLLM → hash.
- Remaining owner action: decide which of the 12 default-false `ENABLE_*` startup gates (`core/startup/agents.py`) should be true in production.

**Status:** core structural defect fixed; flag enablement intentionally left to the owner.
