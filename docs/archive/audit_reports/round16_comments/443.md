## ✅ ফিক্স সম্পন্ন — ৫টির সবগুলো ভাঙা পথ মেরামত/যাচাইকৃত

**বাংলা:** MemoryManager-এর ভুয়া মেথড কল, sync-await TypeError, ফ্যান্টম QDRANT_URL, 1536-dim ড্রিফট, vector_db-র চুপচাপ খালি ফলাফল — সবগুলোর সমাধান main-এ আছে।

**English evidence (commits 2687a346 by parallel agent, 0a132a44, 0fcd0f4f):**
1. `ModelRouter.get_embedding` (AttributeError): removed — embedding now via canonical `core.embeddings.embed_for_pgvector` chain (2687a346). Repo-wide grep: zero remaining callers.
2. sync/await TypeError: `memory_service.save_memory/recall_memories` fixed via `asyncio.to_thread` (2687a346); **`memory/long_term_memory.py` add_memory/retrieve_relevant_memories still broken → fixed in 0a132a44** (supabase-py is sync; the await raised TypeError on EVERY call, browser-agent learnings never persisted).
3. Phantom `settings.QDRANT_URL` (verified AttributeError, silently degraded to Redis-only forever) → safe `getattr` + env fallback in `context_manager.py` (0a132a44); reindex script's `settings.SUPABASE_URL` phantom also fixed.
4. 1536-dim drift: `embed_for_pgvector` normalizes to 384 with warning (2687a346); `core_knowledge_qa.py` no longer requests 1536 (0a132a44); schema contract locked by `tests/models/test_ai_memory_schema_contract.py`.
5. `vector_db.find_similar_experiences` silent `[]` on vector payloads (DEBUG-level lie) → loud WARNING + honest empty (0a132a44).

**Acceptance:** all five claims resolved on main ✓
