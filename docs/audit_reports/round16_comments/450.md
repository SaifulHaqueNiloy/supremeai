## ✅ ফিক্স সম্পন্ন — রানটাইম ক্যাপাবিলিটি ম্যাট্রিক্স এন্ডপয়েন্ট

**বাংলা:** `GET /api/v1/capabilities/runtime` এখন সৎভাবে জানায় এই কনটেইনার আসলে কী কী পারে: প্রতিটি ফিচারের import অবস্থা, free-tier পলিসি-এক্সক্লুশন, ডিগ্রেডেশন নোট, এবং কোন প্রোভাইডার key কনফিগার্ড (শুধু boolean, মান কখনো না)।

**English evidence (commit 77085130):**
- `api/routes/capabilities.py`: `GET /runtime` — per-feature `{feature, available, importable, policy_excluded, degradation_note}` computed via `importlib` at request time (`integrations/_flags.import_available`); `LOW_MEMORY_MODE` correctly reports the local encoder as deliberately-off even when installed; provider-key booleans (gemini/groq/openrouter/openai/cloudflare/elevenlabs/kaggle/runpod/supabase/telegram).
- `policy_excluded` mirrors `scripts/ci/check_free_tier_limits.py` HEAVY_PACKAGES — CI-enforced exclusions are now runtime-visible.
- Auth: per-user token (same as the existing `/capabilities` route); no new router registration needed (mounted at routers.py:60).
- Unblocks #452: frontend already has `controlPlane.capabilities()` + `capabilityAvailable()` consumers.

**Acceptance:** capability truth-source in production ✓ · silent degradations visible ✓
