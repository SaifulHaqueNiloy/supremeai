import React from 'react';
import { Link } from 'react-router-dom';
import { ArrowRight, BookOpen, Check, FileText, History, LockKeyhole, Mail, Play, ShieldCheck, Sparkles, Wrench, Workflow } from 'lucide-react';

const publicLinks = [
  { to: '/features', label: 'Features' },
  { to: '/pricing', label: 'Pricing' },
  { to: '/docs', label: 'Docs' },
  { to: '/about', label: 'About' },
  { to: '/contact', label: 'Contact' },
];

export function PublicHeader() {
  return <header className="sticky top-0 z-20 border-b border-cyan-400/15 bg-[#07111f]/90 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between gap-6 px-5 py-4 sm:px-8"><Link to="/" className="flex items-center gap-3" aria-label="SupremeAI home"><span className="flex size-9 items-center justify-center rounded-lg bg-cyan-300 font-mono text-sm font-bold text-[#07111f]">S</span><span className="font-mono text-sm font-semibold tracking-[0.16em] text-slate-100">SUPREMEAI</span></Link><nav className="hidden items-center gap-6 md:flex" aria-label="Public navigation">{publicLinks.map((link) => <Link key={link.to} to={link.to} className="text-sm text-slate-400 transition hover:text-cyan-200">{link.label}</Link>)}</nav><div className="flex items-center gap-3"><Link to="/login" className="hidden text-sm font-medium text-slate-300 transition hover:text-cyan-200 sm:block">Sign in</Link><Link to="/register" className="rounded-lg bg-cyan-300 px-4 py-2 text-sm font-semibold text-[#07111f] transition hover:bg-cyan-200">Create account</Link></div></div></header>;
}

export function PublicLayout({ children }: { children: React.ReactNode }) {
  return <div className="min-h-screen bg-[#07111f] text-slate-100"><PublicHeader />{children}<footer className="border-t border-cyan-400/15 px-5 py-8 text-sm text-slate-500 sm:px-8"><div className="mx-auto flex max-w-7xl flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><span>SupremeAI — reliable action from intent.</span><span className="font-mono text-xs">Private by design.</span></div></footer></div>;
}

const pageCopy: Record<string, { eyebrow: string; title: string; description: string }> = {
  '/pricing': { eyebrow: 'Plans', title: 'Start small. Govern deeply.', description: 'Explore the operating model first, then bring governed automation to the workflows that matter.' },
  '/features': { eyebrow: 'Capability map', title: 'One command center for intelligent work.', description: 'SupremeAI connects agents, automation, oversight, and execution in one operational surface.' },
  '/docs': { eyebrow: 'Documentation', title: 'Understand the system before you extend it.', description: 'Start with the core concepts, then move into agents, tools, approvals, and deployment.' },
  '/about': { eyebrow: 'The company', title: 'Built for useful autonomy, not opaque magic.', description: 'SupremeAI helps teams turn high-level intent into observable, governable action.' },
  '/contact': { eyebrow: 'Contact', title: 'Bring a real workflow to the command center.', description: 'Tell us what you want to coordinate and we will help map the safest path forward.' },
};

const capabilities = [
  { title: 'Chat', text: 'Turn an outcome into a clear next step.', icon: Sparkles, available: true },
  { title: 'History', text: 'Keep every useful conversation available.', icon: History },
  { title: 'Files', text: 'Ground work in the documents that matter.', icon: FileText },
  { title: 'Tools', text: 'Connect trusted tools and integrations.', icon: Wrench },
  { title: 'Runs', text: 'Execute repeatable workflows with oversight.', icon: Play },
  { title: 'Workspace', text: 'Build, observe, govern, and extend together.', icon: Workflow },
];

export function PublicInfoPage({ kind }: { kind: '/features' | '/pricing' | '/docs' | '/about' | '/contact' }) {
  const copy = pageCopy[kind];
  return <PublicLayout><main className="mx-auto max-w-7xl px-5 py-16 sm:px-8 sm:py-24"><div className="max-w-3xl"><p className="font-mono text-xs uppercase tracking-[0.24em] text-cyan-300">{copy.eyebrow}</p><h1 className="mt-5 text-balance text-4xl font-semibold tracking-tight text-slate-50 sm:text-6xl">{copy.title}</h1><p className="mt-6 max-w-2xl text-pretty text-lg leading-8 text-slate-400">{copy.description}</p></div>{kind === '/features' && <><div className="mt-14 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{capabilities.map(({ icon: Icon, title, text, available }) => <article key={title} className="border border-cyan-400/15 bg-[#0b1a2d] p-6"><Icon className={available ? 'text-cyan-300' : 'text-slate-500'} aria-hidden="true" /><h2 className="mt-8 text-xl font-semibold">{title}</h2><p className="mt-3 leading-7 text-slate-400">{text}</p>{!available && <p className="mt-5 flex items-center gap-2 text-xs font-medium text-slate-500"><LockKeyhole size={14} /> Available with an account</p>}</article>)}</div><Link to="/" className="mt-10 inline-flex items-center gap-2 font-semibold text-cyan-200">Try chat as a guest <ArrowRight size={16} /></Link></>}{kind === '/pricing' && <div className="mt-14 grid max-w-4xl gap-4 md:grid-cols-2">{[{ name: 'Explore', price: 'Free', text: 'Try guest chat and understand the operating model before creating an account.' }, { name: 'Scale', price: 'Custom', text: 'Bring governed automation, integrations, and team workflows to production.' }].map((plan) => <article key={plan.name} className="border border-cyan-400/15 bg-[#0b1a2d] p-7"><p className="font-mono text-xs uppercase tracking-[0.18em] text-cyan-300">{plan.name}</p><p className="mt-5 text-4xl font-semibold">{plan.price}</p><p className="mt-4 leading-7 text-slate-400">{plan.text}</p><Link to={plan.name === 'Explore' ? '/' : '/contact'} className="mt-8 inline-flex items-center gap-2 font-semibold text-cyan-200">{plan.name === 'Explore' ? 'Try guest chat' : 'Talk to us'} <ArrowRight size={16} /></Link></article>)}</div>}{kind === '/docs' && <div className="mt-14 grid max-w-4xl gap-4 md:grid-cols-2">{['Start with intent', 'Add context', 'Connect tools', 'Approve and run'].map((item, index) => <article key={item} className="border border-cyan-400/15 bg-[#0b1a2d] p-6"><p className="font-mono text-xs text-cyan-300">0{index + 1}</p><h2 className="mt-6 text-xl font-semibold">{item}</h2><p className="mt-3 leading-7 text-slate-400">A practical stage in moving from a conversation to an observable, governed workflow.</p></article>)}</div>}{kind === '/about' && <div className="mt-14 max-w-3xl border border-cyan-400/15 bg-[#0b1a2d] p-7"><ShieldCheck className="text-cyan-300" aria-hidden="true" /><p className="mt-6 text-lg leading-8 text-slate-300">The product is designed around a simple belief: powerful automation should remain understandable, interruptible, and accountable to the people using it.</p></div>}{kind === '/contact' && <div className="mt-14 max-w-2xl border border-cyan-400/15 bg-[#0b1a2d] p-7"><Mail className="text-cyan-300" aria-hidden="true" /><p className="mt-6 leading-7 text-slate-400">Share the workflow you want to improve and our team will help you identify the right starting point.</p><a href="mailto:hello@supremeai.example" className="mt-8 inline-flex items-center gap-2 font-semibold text-cyan-200">Email the team <ArrowRight size={16} /></a></div>}</main></PublicLayout>;
}

export function PricingPage() { return <PublicInfoPage kind="/pricing" />; }

type GuestMessage = { role: 'user' | 'assistant'; text: string };

function previewResponse(text: string) {
  const lower = text.toLowerCase();
  if (lower.includes('file') || lower.includes('document')) return 'That sounds like a context-rich workflow. In a full workspace, you can attach files and keep the source material connected to the run.';
  if (lower.includes('team') || lower.includes('approve')) return 'That is a good fit for governed collaboration. You can map approval points, assign ownership, and keep an audit trail once you create a workspace.';
  if (lower.includes('automat') || lower.includes('workflow')) return 'I can see the workflow shape: define the outcome, add the right tools, then run it with human approval where it matters.';
  return 'That is a useful starting point. I can help you clarify the outcome, context, and next action before you decide whether to create an account.';
}

export function GuestChatPage() {
  const [messages, setMessages] = React.useState<GuestMessage[]>([{ role: 'assistant', text: 'Welcome. Describe something you want to accomplish and I will help you shape the next step. This temporary session is available without an account.' }]);
  const [input, setInput] = React.useState('');
  const [showGate, setShowGate] = React.useState(false);
  const submit = (event: React.FormEvent) => { event.preventDefault(); const text = input.trim(); if (!text) return; setMessages((current) => [...current, { role: 'user', text }, { role: 'assistant', text: previewResponse(text) }]); setInput(''); setShowGate(true); };
  return <PublicLayout><main className="mx-auto max-w-7xl px-5 py-10 sm:px-8 sm:py-16"><div className="grid items-start gap-10 lg:grid-cols-[0.75fr_1.25fr] lg:gap-16"><section className="lg:sticky lg:top-28"><p className="font-mono text-xs uppercase tracking-[0.24em] text-cyan-300">Guest command center</p><h1 className="mt-5 text-balance text-5xl font-semibold tracking-tight sm:text-6xl">Experience the system before you configure it.</h1><p className="mt-6 max-w-xl text-pretty text-lg leading-8 text-slate-400">Chat through a real temporary session, then unlock the context and execution layers when you are ready.</p><div className="mt-8 flex flex-wrap gap-3 text-sm text-slate-400"><span className="flex items-center gap-2"><Check size={15} className="text-cyan-300" /> No account to start</span><span className="flex items-center gap-2"><Check size={15} className="text-cyan-300" /> Temporary by default</span></div><div className="mt-10 grid grid-cols-2 gap-2 sm:grid-cols-3 lg:grid-cols-2">{capabilities.map(({ title, icon: Icon, available }) => <div key={title} className={available ? 'border border-cyan-300/40 bg-cyan-300/10 p-3' : 'border border-white/10 bg-[#0b1a2d] p-3'}><Icon size={16} className={available ? 'text-cyan-300' : 'text-slate-500'} aria-hidden="true" /><p className="mt-3 text-sm font-medium">{title}</p><p className="mt-1 text-xs text-slate-500">{available ? 'Try now' : 'Sign in to unlock'}</p></div>)}</div></section><section className="border border-cyan-400/20 bg-[#0b1a2d] shadow-2xl shadow-cyan-950/30" aria-labelledby="guest-chat-heading"><div className="flex items-center justify-between border-b border-cyan-400/15 px-5 py-4"><div><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-cyan-300">Temporary session</p><h2 id="guest-chat-heading" className="mt-1 font-semibold">Intent workspace</h2></div><span className="flex items-center gap-2 text-xs text-slate-500"><span className="size-2 rounded-full bg-emerald-400" /> Guest mode</span></div><div className="flex min-h-80 flex-col gap-4 p-5">{messages.map((message, index) => <div key={`${message.role}-${index}`} className={message.role === 'user' ? 'ml-8 border border-cyan-400/20 bg-cyan-300/10 p-4 text-sm text-cyan-50' : 'mr-8 border border-white/10 bg-[#07111f] p-4 text-sm leading-6 text-slate-300'}>{message.text}</div>)}{showGate && <div className="border border-cyan-400/20 bg-cyan-300/5 p-4"><p className="flex items-center gap-2 text-sm font-medium text-cyan-100"><History size={16} /> Want to keep this conversation?</p><p className="mt-2 text-sm leading-6 text-slate-400">Create an account to save history, add files, connect tools, and continue inside your workspace.</p><div className="mt-4 flex flex-wrap gap-3"><Link to="/register" className="inline-flex items-center gap-2 rounded-lg bg-cyan-300 px-4 py-2 text-sm font-semibold text-[#07111f]">Save and continue <ArrowRight size={15} /></Link><Link to="/login" className="inline-flex items-center gap-2 rounded-lg border border-cyan-400/25 px-4 py-2 text-sm font-semibold text-cyan-100">Sign in</Link></div></div>}</div><form onSubmit={submit} className="border-t border-cyan-400/15 p-4"><label htmlFor="guest-message" className="sr-only">Message SupremeAI</label><div className="flex flex-col gap-3 sm:flex-row"><textarea id="guest-message" value={input} onChange={(event) => setInput(event.target.value)} placeholder="Describe an outcome, workflow, or problem..." className="min-h-20 flex-1 resize-none rounded-lg border border-white/10 bg-[#07111f] p-3 text-sm text-slate-100 outline-none focus:border-cyan-300" rows={2} /><button type="submit" disabled={!input.trim()} className="rounded-lg bg-cyan-300 px-5 py-3 text-sm font-semibold text-[#07111f] transition hover:bg-cyan-200 disabled:cursor-not-allowed disabled:opacity-50">Send message</button></div><p className="mt-3 text-xs text-slate-500">Guest conversations are temporary and remain in this browser session only.</p></form></section></div></main></PublicLayout>;
}

export default GuestChatPage;
