## ✅ ফিক্স হয়েছে — commit `7feb0700` (production-এ live: c204db97)

**বাংলা:** Upstash কোটা শেষ হওয়ায় প্রতিটি রিকোয়েস্টে ব্যর্থ Redis কল হচ্ছিল (লগের ৪২–৫১% ছিল fallback warning)। এখন `SecureRedisManager`-এ quota circuit breaker — কোটা-এরর ধরা পড়লেই ১৫min → ৩০min → ১h ব্যাকঅফে ব্রেকার ওপেন; সব কনজিউমার তখন নিজের documented in-memory fallback-এ যায়, **কোনো নেটওয়ার্ক কল হয় না**; প্রথমবার একটাই CRITICAL ঘোষণা। প্রতি-রিকোয়েস্ট warning এখন ৫ মিনিটে ১ বার।

**English:** `core/cache/redis_manager.py`: quota-aware breaker (`report_failure()` classification → `get_client_async()` returns None while open — same fail-closed contract as `memory://`); wired into manager ops + both hot middleware consumers (`core/middleware/security.py`, `middleware/rate_limiter.py`). Verified: 6-assertion functional smoke (trip/cooldown/None-while-open/half-open/escalation/no-false-positive), ruff clean.

**Owner action (code দিয়ে সম্ভব নয়):** Upstash plan upgrade বা monthly reset — breaker waste বন্ধ করে, কিন্তু aggregate-safe rate limiting ফিরতে Redis দরকার।
