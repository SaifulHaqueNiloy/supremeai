## 🔧 আংশিক ফিক্স ল্যান্ড করেছে — commit `dd40ed9e` (production-এ live)

**বাংলা:** (১) Blueprint save এখন সত্যিই পারসিস্ট হয় (`data/swarm_blueprints/*.json`, validated flow_id, সত্যিকারের 404); (২) execute এখন blueprint লোড+ভ্যালিডেট করে, তারপর সৎ **501 SWARM_EXECUTION_NOT_IMPLEMENTED** (আগে যেকোনো id-তে বানানো "executed successfully"); (৩) ফ্রন্টএন্ড এখন সঠিক পথে কল করে (`/api/v1/evolution/swarm/forge` — আগের `/api/v1/swarm/forge` 404 দিত), সেভ করা flow_id ধরে রাখে, এবং ব্যাকএন্ডের সৎ 501 বার্তা ইউজারকে দেখায়।

**English:** `evolution.py` save/execute rewritten; `EvolutionForge.tsx` handlers fixed. **Remaining (issue stays open):** `/api/v1/swarm/stream` still absent from the live router (frontend calls it); simulated swarm graph telemetry needs a real implementation.
