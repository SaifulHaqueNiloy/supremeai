# SupremeAI Mesh Node Daemon — runtime dependencies
# MESH-3 (issue #941)
# PC-1 (Dev Rig) এবং PC-2 (Headless Tester) এ ইনস্টল করতে হবে।
# Install: python3 -m pip install -r requirements.txt

# WebSocket transport — Tower-এর সাথে persistent connection
websockets>=12.0,<14.0

# HTTP client — heartbeat POST এবং ollama /api/generate call করতে
httpx>=0.27,<1.0

# YAML config লোড করার জন্য
PyYAML>=6.0,<7.0

# সিস্টেম load (cpu/mem/active_tasks) রিপোর্ট করতে
psutil>=5.9,<7.0

# টেস্ট dependencies (production-এ optional)
pytest>=7.4,<9.0
pytest-asyncio>=0.23,<1.0
