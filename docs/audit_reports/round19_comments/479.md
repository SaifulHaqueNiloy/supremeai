## ✅ Resolved — ai_memory re-index verified: 588/588 semantic, 0 legacy/hash remaining

### Independent verification performed today (2026-09-19, commit 0925dc0+)

The full re-embedding sweep (`scripts/maintenance/reindex_ai_memory_embeddings.py`, canonical model `@cf/baai/bge-small-en-v1.5`, **384-dim = the platform's canonical `_PG_DIM`** — the "768 or 1536" in this issue was speculative; the pgvector column and the entire provider chain are hard-canonicalized to 384) had been executed earlier today: dry-run first, then live — **588/588 rows re-indexed, 0 failures**. Rather than re-run it blindly, today's session verified the *stored state* directly:

**1. Hash-vs-semantic vector audit (all 588 rows)**
Recomputed the deterministic `hash_vectorize()` fallback for every row's content and compared against the stored embedding (blake2b-stable, process-independent):
| metric | result |
|---|---|
| total rows | 588 |
| **semantic vectors (≠ deterministic hash vector)** | **588 (100%)** |
| hash-fallback vectors (== hash vector) | 0 |
| wrong dimension | 0 |
| empty vector / empty text | 0 / 0 |
| rows created after the sweep | 0 |
| cosine(stored, hash-vector) histogram peak | ≈ 0.00 (orthogonal — expected for semantic vs lexical vectors) |

**2. Nearest-neighbor validation (acceptance criterion 4)**
Via the production `match_memories` pgvector RPC: querying with a row's own stored embedding returns that row with **similarity = 1.0** (top-K = 3/20 both confirmed). The search path, dimension compatibility and index are healthy — no rollback needed, so criterion 5 (keep old vectors until validation) is satisfied by the completed-and-validated migration.

**3. Tenant boundaries (criterion 3)** — only the `embedding` column was ever updated (`UPDATE … SET embedding` by id); `user_id`/`session_id`/metadata untouched; no code changes shipped for this closure → zero regression surface. Note: all sampled rows carry `user_id = NULL` (global memories), so cross-tenant isolation is unaffected.

**4. Idempotency/resume (criterion 5)** — the script is batch-ordered by `created_at` offset, overwrites in place, and reports auditable counts (588 processed / 588 semantic / 0 hash / 0 failed).

### ⚠️ Follow-up discovered during verification → GAP-009 (owner)
`CLOUDFLARE_API_TOKEN` in the Render primary **and** worker env now tests **401 Invalid API Token** (it was live at the sweep). Until rotated, NEW ai_memory rows silently degrade to the hash embedder (fail-soft, loudly announced in logs). Recorded as **GAP-009 `[BLOCKED:OWNER]`** in `docs/audits/ACTIVE_AUDIT_QUEUE.md`.

### 🇧🇩 বাংলা সারসংক্ষেপ
৫৮৮/৫৮৮ রো ক্যানোনিকাল সেমান্টিক ভেক্টর — হ্যাশ-ফলব্যাক শূন্য, ডাইমেনশন ৩৮৪-এ সব ঠিক। pgvector `match_memories` RPC-তে self-match similarity 1.0 প্রমাণিত। টেন্যান্ট মেটাডেটা অক্ষত, মাইগ্রেশন অডিটেবল/রিজিউমেবল। সতর্কতা: Render-এর `CLOUDFLARE_API_TOKEN` এখন 401 — নতুন রো হ্যাশ-এ পড়ার ঝুঁকি; GAP-009 হিসেবে ওনারের জন্য রেকর্ড করা হয়েছে। ইস্যুটি বন্ধ হচ্ছে।
