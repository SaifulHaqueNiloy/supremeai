# QA Engine — E2E Customer suite (Part 7.2 of the QA engine plan, adapted)
# Deprecated: consolidated into e2e-suites.yml (Wave 3.5).
# Retained as a delegation wrapper for Preservation Guard compliance.
name: QA — E2E Customer Suite (Deprecated Shim)

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: supremeai-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run-customer-suite:
    name: 🎭 Customer Suite
    uses: ./.github/workflows/reusable-e2e-runner.yml
    with:
      project: customer
      use_staging: true
    secrets: inherit
