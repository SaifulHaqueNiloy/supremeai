diff a/backend/tests/services/test_services_internet_monitor.py b/backend/tests/services/test_services_internet_monitor.py	(rejected hunks)
@@ -277,8 +277,13 @@ async def test_service_lifecycle():
     if service.monitoring_task:
         service.monitoring_task.cancel()
         # Suppress the CancelledError for this test
+        # বাংলা মন্তব্য (ROOT-CAUSE FIX): CancelledError, Exception-এর subclass
+        # না, তাই আগে `except Exception` নিজেদেরই cancel() করা এই expected
+        # CancelledError ধরতে পারত না।
         try:
             await service.monitoring_task
+        except asyncio.CancelledError:
+            pass
         except Exception as e:
             import logging
 
