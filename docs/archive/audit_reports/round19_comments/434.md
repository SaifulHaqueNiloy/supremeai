## 📈 Round 19 progress — raw-path contract now documented, tested, and weekly-guarded (2/3 criteria met)

### What shipped (commits 0aba876 → a0442fe)

**1. Integrator-facing doc note merged (acceptance ✅)** — `docs/guides/infisical_vault_integration_guide.md`:
- States plainly: **do NOT use the standard v1 `listSecrets()` for this project** (blind-index corruption → 404/empty with identity auth).
- Documents the **v3 raw API** as the only supported interface — with the exact routes, auth, and env slugs the production backend uses.

**2. Weekly synthetic check shipped (part of acceptance ✅ #3)** — `scripts/maintenance/check_infisical_vault_synth.py` + `maintenance.yml` → `infisical-vault-synthetic-check` job (weekly cadence):
- Asserts: universal-auth login ✓, raw listing **≥ 100 prod secrets** ✓, boot-critical keys served via raw get-by-key ✓.
- Runs **verified live today**: `PASS: raw listing -> 177 prod secrets`, `PASS: raw get-by-key serves SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY` — the v1 route still 404 (informational).
- If the raw path ever breaks → the job goes red **before** a Sep-14-style silent fail-closed crash can happen.
- The script also reports when v1 starts returning 200 → the deliberate "remove workaround" trigger from this issue's checklist.

**3. Raw-path contract is now the documented, tested interface (acceptance ✅ #3)** — the issue allowed either "standard API returns 139 secrets" OR "the raw-path contract is the documented, tested interface". The latter is now true: documented in the guide, asserted weekly, and matched to production's actual SDK path.

### The remaining criterion is owner-only (acceptance ⏳ #1)
**Vendor ticket URL** requires the owner's Infisical account — agents cannot open support tickets. Ready-to-paste draft:

> **Subject:** REST API 404/empty for standard secret-list routes with universal-auth identity (project 92aa20c4-aef5-4e33-82bd-efb06058aaf0)
> **Body:** Since ~Sep-14, `GET /api/v1/secrets?workspaceId=…&environment=prod` with a valid universal-auth accessToken returns `404 {"message":"Route GET:… not found"}` (and `/api/v1/secrets/raw?…` in v1 returns empty), while `GET /api/v3/secrets/raw?…` returns 177 secrets and raw get-by-key works. Identity: universal-auth, client id `9f2363cf-…`. This looks like a blind-index corruption on the v1 listing route. We need the v1 route restored (or an official statement that v3 raw is the supported contract).

**Issue stays OPEN until the owner posts the ticket URL** (honest per the acceptance list) — everything engineerable is done and verified.

### 🇧🇩 বাংলা সারসংক্ষেপ
ইন্টিগ্রেটর গাইড মার্জ হয়েছে (v1 listSecrets নিষিদ্ধ; v3 raw-ই সংযোগপথ), সাপ্তাহিক সিনথেটিক চেক লাইভ যাচাইকৃত (১৭৭টি সিক্রেট, বুট-কী পাওয়া যাচ্ছে)। একমাত্র অবশিষ্ট: ওনারের অ্যাকাউন্ট থেকে ভেন্ডর টিকিট — টিকিটের প্রস্তুত খসড়া এই কমেন্টেই।
