## ✅ ফিক্স হয়েছে — commit `dd40ed9e` (production-এ live: c204db97)

**বাংলা:** আগে STT সব অডিওর জন্য একই বাঙলা ট্রান্সক্রিপ্ট (confidence 0.96) আর TTS ফাঁকা RIFF WAV বাইট স্ট্রিম করত। এখন: STT = আসল Groq Whisper (`whisper-large-v3`, websocket_voice-এর একই চুক্তি); TTS = edge-tts (ফ্রি, কী-লেস, বাঙলা bn-BD ভয়েস) ইনস্টল থাকলে, নইলে ElevenLabs (কী থাকলে), নইলে সৎ `STT_NOT_CONFIGURED` / `TTS_NOT_CONFIGURED`। SSE রুট (`stream_voice_sse.py`) আর কখনো placeholder বাইট দেয় না — `event: error` সহ কী করতে হবে তা বলে।

**English:** `voice_service.py` rewritten (real provider chain, honest unavailable statuses, no fabricated confidence/language fields); SSE error events carry `reason` + `message`. py_compile + ruff clean. Note: SSE voice contract-এর কোনো frontend consumer নেই (WS `/ws/voice`-ই লাইভ পথ) — তাই prod-এ user-facing প্রভাব শূন্য ছিল।
