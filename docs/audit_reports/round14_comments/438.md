## ✅ ফিক্স হয়েছে — commit `7f0dfa82` (production-এ live: c204db97)

**বাংলা:** OpenRouter-এর অবৈধ কী ৪০১ দিতেই থাকত — দুই স্ট্যাকই এটাকে সাধারণ এরর ভাবত। এখন: (১) `dynamic_ai` health-check-এ ৪০১/৪০৩ = কী চূড়ান্তভাবে অবৈধ → `DISABLED_PERMANENT` (refresh loop permanent skip করে — probe storm বন্ধ); রিকোয়েস্ট-টাইম ৪০১/৪০৩-এও সাথে সাথেে permanent disable। (২) `llm_gateway` key-pool: flat ৩০০s cooldown বাদ — ৫min → ১h → ২৪h এসক্যালেটিং cooldown প্রতি (provider,key)-এ; সফল কলে reset (`mark_success` wired)।

**English:** `services/dynamic_ai/provider_registry.py` (`_validate_api_key` now returns HTTP status; 401/403 → permanent + actionable last_error "rotate <ENV VAR>"), `core/llm/llm_gateway/registry.py` (`_AUTH_COOLDOWN_ESCALATION = (300, 3600, 86400)`), `completion.py` (mark_success on healthy call). py_compile + ruff clean.

**Owner action:** নতুন OpenRouter key vault-এ rotate করুন — provider আবার চালু হতে হলে বৈধ কী লাগবে (কী বদলালে restart বা enable_provider())।
