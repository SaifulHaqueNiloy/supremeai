## ✅ Resolved — Critical invariant guard + dedicated atomic Lua invariant tests

### Shipped (commit 38e65f9)

**1. Critical Invariant Guard — `scripts/ci/check_critical_invariants.py`**
Data-driven registry: each invariant = (file, required literal markers, why-it-is-critical). A missing FILE or missing marker **fails the build** with an actionable report pointing at the likely merge/rebase drop. Seeded with 7 verified targets:
| invariant | markers |
|---|---|
| Atomic Lua sliding-window (middleware) | `ZREMRANGEBYSCORE ZCARD ZADD EXPIRE` in ONE script |
| Atomic Lua sliding-window (security) | same atomicity contract |
| Global rate-limit middleware | `RateLimitMiddleware` registration surface |
| Tenant-scoped idempotency | `scoped_key` + `idempotency:response:` (cross-tenant replay fix) |
| Production readiness schema gate (#478) | required-table registry |
| Role-aware DB degradation policy | `db_failure_readiness` (fail-closed core/production) |
| Vault fail-closed contract | `ProductionSecretVault` + `SecretNotFoundError` |

**Verified both directions**: green when intact (`✅ 7 invariant targets verified`) and **exit 1** when a marker is deliberately removed (negative test executed during development). If an invariant genuinely moves, the registry is updated in the SAME commit with a link to the issue — the guard stays an honest signal, never a ratchet of shame.

**2. Wired into two enforcement points**
- CI: `ci.yml` → `tooling-quality` job → new step (runs on every backend/infra change).
- Developer-side: `.pre-commit-config.yaml` → **pre-push** hook (exactly the "pre-push hook গুরুত্বপূর্ণ কোর মডিউল আনইনটেনশনাল ডিলিশন স্ক্যানার" the checklist asked for).

**3. Dedicated atomic invariant tests — `backend/tests/core/test_rate_limiter_lua_invariants.py` (4/4 green)**
- Structural (always run): Lua script contains ALL atomic ops in one script; NO non-atomic `GET`/`SET`/`DEL` decide-then-write flow; the allow/deny decision happens INSIDE the script.
- **Observable atomicity** (fakeredis+lupa; skips cleanly when absent): 50 concurrent callers against the PRODUCTION eval arg-order, limit=5 → **EXACTLY 5 allowed**. A de-atomized limiter under concurrent load fails this loudly.

### 🇧🇬 বাংলা সারসংক্ষেপ
ক্রিটিকাল ইনভেরিয়েন্ট গার্ড যোগ হয়েছে — ৭টি টার্গেট (অ্যাটমিক Lua রেট লিমিটার, টেন্যান্ট-স্কোপড আইডেমপোটেন্সি, স্কিমা গেট, হেলথ পলিসি, ভল্ট fail-closed)। মার্কার মিসিং = বিল্ড ফেল + অ্যাকশনেবল রিপোর্ট। CI (tooling-quality) ও pre-push hook — দুই জায়গায় এনফোর্সড। আলাদা ইনভেরিয়েন্ট টেস্ট ফাইলে ৪/৪ গ্রিন: ৫০ কনকারেন্ট কলে ঠিক ৫টি allow — অ্যাটমিসিটি এখন পর্যবেক্ষণযোগ্যভাবে প্রমাণিত। চেকলিস্টের দুটি আইটেমই সম্পন্ন।
