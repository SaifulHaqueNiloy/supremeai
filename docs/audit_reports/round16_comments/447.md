## ✅ ফিক্স সম্পন্ন — নকল ব্রাউজার ফলব্যাক সম্পূর্ণ বাদ; শূন্য সোর্স মানে সৎ রিপোর্ট

**বাংলা:** "Autonomous browser" ফলব্যাকটা আসলে স্ক্রিপ্ট ছিল — যে কোনো প্রশ্নে নির্দিষ্ট ৩ ধাপ (supremeai.dev ট্যুর) চলত, ব্রাউজার কিছুই চালাত না, তবুও "cited-looking" রিপোর্ট বানাতো। এখন ফলব্যাক সম্পূর্ণ বাদ: সোর্স না পেলে স্পষ্ট `no_web_sources: true` সহ সৎ রিপোর্ট যায়, synthesis prompt-এ নিষেধ করা আছে বানোয়াট সাইটেশন।

**English evidence (commit 3062fd37):**
- `deep_research.py _web_search`: the `AutonomousBrowserAgent` fallback deleted (its `_decide_action` ignored the goal — hardcoded navigate supremeai.dev → smart_click → done; `navigate` never touched a browser; response could never parse as JSON anyway).
- Zero-source path: Steps 8-9 guarded — honest "No Sources Retrieved" report with `no_web_sources: True` instead of LLM-authored fabrication; synthesis prompt adds "Only cite sources that appear in the findings list; say so explicitly instead of inventing information."
- Real primary path untouched: scout crawler stays strictly CrawlPolicy-gated (robots/pacing/SSRF).
- Frontend (`DeepResearchPanel.tsx`): the panel never rendered live steps/final report (expected `step_update`/`complete`, backend emits `step`/`report`) — real contract now accepted; also switched `window.location.origin` → `getApiBaseUrl()`.

**Acceptance:** zero fake-browser path ✓ · zero-source honesty ✓ · report actually renders ✓
