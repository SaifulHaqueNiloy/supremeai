## 📋 Round 19 status — canonical state transition documented; owner ratification remains

### Current enforced semantics (from code + existing tests, verified today)
- **State machine**: `PENDING → APPROVED | REJECTED | CANCELLED`, with **expiry** (`TaskExpiredError` → HTTP 410) and **replay/tamper/race guards** (AUD-4.3–4.6 → HTTP 409) — `api/routes/approval_manager.py::approve_task` + `models/pending_tasks.py`.
- **Approve ≠ execute** for the general contract: `approve_task` moves the record to `APPROVED`; execution is dispatched by the task-type-specific handlers (e.g., `SKILL_GENERATION` executes inline inside the approve call — the one place where approve-then-execute is coupled, by design and documented in code).
- **Integrity**: every record carries a canonical SHA-256 `payload_hash` (`runs/hitl.py::_payload_hash`, AUD-4.4 discipline); HITL engine mirrors to canonical `pending_tasks` (M17-P-D, landed today in #453 Wave 4 — commit `05ccb8c`, with `backend/tests/services/test_hitl_canonical_mirror.py`).

### What this issue still needs (honest status)
1. **Owner ratification** of the above as the CANONICAL contract — including the explicit question this issue raises: inline execute-on-approve for `SKILL_GENERATION` is currently the only approve-executes coupling; either ratify it as intentional or schedule its decoupling. (HITL decision per the issue's own title — cannot be auto-ratified by an agent.)
2. A dedicated **contract test file** pinning the full transition table (PENDING→APPROVED→executed; PENDING→REJECTED; expired→410; double-decision→409; tampered hash→409) so the semantics are regression-proof. Note: `backend/tests/hitl/test_hitl_engine.py` (700+ lines) already covers large parts of this — the ask reduces to consolidating the transition-table assertions in one place. NOT started in this round to avoid colliding with the in-flight M17-P-D HITL mirror work by the parallel agent.

### 🇧🇩 বাংলা
স্টেট-মেশিন ডকুমেন্ট করা হলো (PENDING→APPROVED/REJECTED/CANCELLED, 410/409 guards)। ওনারের র‍্যাটিফিকেশন বাকি — বিশেষত SKILL_GENERATION-এর approve-এর সাথে inline execute কাপলিংটা ইচ্ছাকৃত কিনা। কন্ট্রাক্ট টেস্ট কনসোলিডেশন M17-P-D-এর সমান্তরাল কাজ শেষ হলে করা ভালো (কলিশন এড়াতে)।
