# NOTE: frozen-lockfile is a pnpm-only option and must NOT be set here.
# npm equivalent is enforced via `npm ci` in CI jobs directly.
# pnpm frozen-lockfile behaviour is configured in pnpm-workspace.yaml / .pnpmfile.cjs.
