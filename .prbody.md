Fixes Class M "gates that don't gate" from SYSTEM_DEFECT_REGISTER_2026-09-15.

## ERR-M01 — scanner blind to the actual stub classes
`find_stub_data.py` knew only 20 literal regexes and printed `[PASS]` over **1,157 backend stub hits**. Hardened detection now covers: fabricated "Mock output for execution" strings, `mock_session_\d+` fabrication, frozen-timestamp telemetry (ERR-G07 regression guard), fabricated success toasts (`setTimeout(... '✅ ...')`), canned agent placeholders ("Investigation complete.", "Fix applied."), `Math.random()` / `random.uniform` metric fabrication, `provider="mock"` adapters, `mock: True` returns, and stub/not-implemented markers.

Honest fail-labeled behavior is whitelisted (fail-honest ≠ stub): `CloudSandboxOrchestrator` mock-labeled output (ERR-G04) and the now-honest `MockMessagingAdapter`.

## ERR-M02 — gate not wired into CI
New **Stub Blocker (Zero-Gap Gate)** CI job runs the scanner over `backend/`, `frontend/src/`, `scripts/` with `--fail-on HIGH`, so stub code can no longer merge unblocked.

## Caught immediately by the hardened gate (real stubs on main, fixed here)
1. `TaskRunnerAgent` pipeline steps claimed "Investigation complete." / "Fix applied." with zero work → now explicitly "NOT PERFORMED / NOT APPLIED: no tooling wired".
2. `MockMessagingAdapter` returned fabricated `success=True` delivery → now `success=False` + explicit "message was NOT delivered" error.
3. `UnifiedChatBubble` run action fabricated "✅ Executed!" after 1.5s → real backend sandbox execution with the actual exit code (mirrors the ActionCard fix in #386).

**Verification:** backend + frontend + scripts scans all PASS at HIGH threshold; 467 existing tests pass locally.
