- [x] **1. Centralized Route Registration**
  - [x] Clean up `backend/main.py` to avoid duplicate registrations with `core/app.py`.
  - [x] Remove duplicate router registrations in `backend/core/app.py` (`auth`, `evolution`, `onboarding`).
  - [x] Add `swarm` router to `core/app.py` to make it reachable.
- [x] **2. Fix Route Conflicts**
  - [x] Modify `api/routes/task_workspace.py` prefix to avoid conflict with `task.py` (e.g. use `/workspace/task`).
- [x] **3. Fix Swarm Limiter & Routing**
  - [x] Update `api/routes/swarm.py` to use the global SlowAPI limiter correctly, or remove the local instance.
- [x] **4. Merge Billing and Payments**
  - [x] Merge `api/routes/payments.py` checkout and webhook logic into `api/routes/billing_api.py`.
  - [x] Remove `payments.py` from `core/app.py` registration.
  - [x] Update `billing_api.py` to use `Depends(get_current_user_token)` instead of hardcoded `"default_user_session"`.
  - [x] Add a data migration script to handle old `"default_user_session"` records.
- [x] **5. Fix `auth.py` Production Login**
  - [x] Remove the 501 Error block in `api/routes/auth.py` to allow direct login in production with fallback logic.
- [x] **6. Fix `evolution.py` Dependency**
  - [x] Implement `FitnessEngine` as a Singleton in `api/dependencies.py`.
  - [x] Update `api/routes/evolution.py` to use `Depends(get_fitness_engine)`.
- [x] **7. Fix `admin.py` Path**
  - [x] Use absolute path for `AdminGodLayer` DB initialization in `api/routes/admin.py`.
- [x] **8. Verification**
  - [x] Run `alembic check`.
  - [x] Write integration tests for `billing_api`.
