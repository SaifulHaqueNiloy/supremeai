## ✅ ফিক্স সম্পন্ন — SSE রুট নিবন্ধিত + সত্যিকারের টেলিমেট্রি

**বাংলা:** ফ্রন্টএন্ড EvolutionForge যে `/api/v1/swarm/stream` কল করত, সেটি router-এ ছিলই না (Phase-1 consolidation-এ মুছে গিয়েছিল) — এখন Redis-backed SwarmPubSub-এর সাথে SSE bridge যুক্ত, DebateOverlay সত্যিকারের debate ইভেন্ট দেখবে। Swarm Map-কে যে নকল prototype গ্রাফ খাওয়ানো হতো, সেটিও বাদ — এখন সত্যিকারের agent_supervisor health map।

**English evidence (commit fe7885a4):**
- NEW `backend/api/routes/swarm_stream.py`: `GET /api/v1/swarm/stream` SSE bridging `get_swarm_streamer().subscribe()` (multi-worker safe) with `: ping` keepalives; registered in `api/routers.py` ALL_ROUTERS.
- Debate events are exactly what `engine/debate_engine.py` broadcasts (`{"type": "DEBATE_UPDATE", ...}`) — zero simulation.
- `evolution.py /swarm-graph`: hardcoded prototype nodes ("Code-Optimizer teaches FastAPI Refactor") replaced with live `agent_supervisor.get_health()` nodes + honest empty edges + `"simulated": false`.
- Forge save/execute half was already fixed in Round 14 (real JSON persistence + honest 501).

**Acceptance:** route live ✓ · real telemetry source ✓ · simulated graph gone ✓
