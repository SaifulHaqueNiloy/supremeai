# Issue #541 (BE-09): uvicorn pin aligned with backend/pyproject.toml
# (`uvicorn = {extras = ["standard"], version = "^0.51.0"}`, poetry.lock 0.51.0).
# The worker intentionally runs a minimal standalone stack (see main.py: it
# talks to Core API over HTTP and imports only redis/aiohttp/httpx — no
# FastAPI/SQLAlchemy/pydantic), so it does not consume backend pyproject.toml;
# only the uvicorn version must not diverge from the rest of the fleet.
uvicorn[standard]==0.51.0
httpx==0.28.1
redis[hiredis]==5.2.1
aiohttp>=3.13.3
